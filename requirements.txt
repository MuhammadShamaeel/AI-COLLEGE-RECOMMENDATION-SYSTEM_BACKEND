# Core Django and REST Framework
Django==6.0.5
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.4.0

# Database
psycopg2-binary==2.9.10

# Server and Production Tools
gunicorn==22.0.0
whitenoise==6.7.0
python-decouple==3.8

# Authentication
PyJWT==2.9.0

# Utilities
sqlparse==0.5.1
requests==2.32.3
python-dotenv==1.0.1

# For CSV processing (if needed)
pandas==2.2.3

# Note: The following packages are REMOVED for Render deployment:
# - torch, transformers, sentence-transformers (too large - 2+ GB)
# - faiss-cpu, langchain, langchain-* (requires heavy compute)
# - ollama (cannot run on Render free tier)
# - numpy, scipy, scikit-learn (optional, add if needed)